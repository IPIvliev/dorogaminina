<%= form_for(@zveno) do |f| %>
  <% if @zveno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@zveno.errors.count, "error") %> prohibited this zveno from being saved:</h2>

      <ul>
      <% @zveno.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Номер звена" %><br />
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label "Всего мест" %><br />
    <%= f.number_field :vse, :min => 1 %>
  </div>
  <div class="actions">
    <%= f.submit :value => "Сохранить" %>
  </div>
<% end %>
